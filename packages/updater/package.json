{
  "name": "@repo/updater",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/main.ts",
  "types": "./src/main.ts",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/updater.mjs --log-level=info",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@types/node": "^24.0.6",
    "esbuild": "^0.28.1",
    "typescript": "catalog:",
    "vitest": "^4.1.6"
  }
}
