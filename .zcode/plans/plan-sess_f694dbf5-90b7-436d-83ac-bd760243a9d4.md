Implement a subtle SwiftUI haptic response for calculator keypad interaction in `apple/Qianlai/Qianlai/Shared/UI/CalculatorView.swift`.

- Add a private `@State` counter/token on `CalculatorView` used solely as the sensory-feedback trigger.
- Route all keypad actions (digits, decimal, backspace, arithmetic operators, and the check/save key) through a small local helper that performs the existing action and increments the trigger, keeping calculator engine behavior unchanged.
- Attach `.sensoryFeedback(.selection, trigger: token)` to the calculator view so each accepted tap produces a light selection-style haptic on supported Apple platforms. Keep the existing `KeyPressStyle` visual animation and disabled commit behavior intact.
- Run the relevant Qianlai test/build command available in the repository, reporting any platform/toolchain limitations. No engine tests need changes because this is view-only feedback.