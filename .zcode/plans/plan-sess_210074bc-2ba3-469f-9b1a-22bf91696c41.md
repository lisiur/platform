Implement the minimal visual fix in two layers:

1. Update `apple/Qianlai/Qianlai/Journal/EntryListView.swift` so the root `List` hides the platform-provided scroll background and explicitly paints `Color.groupedCanvas`. This is the shared list used by both Journal and Dashboard, and `groupedCanvas` is already the app’s intended page canvas color.
2. Update `apple/Qianlai/Qianlai/Dashboard/DashboardView.swift` so the outer dashboard content has the same `Color.groupedCanvas` background, covering the short interval before the ledger/project branch resolves and preventing the loading or empty-state branches from exposing the window background.
3. Run the Qianlai iOS build/check command available in the project, and inspect the diff to ensure the change is limited to background stability with no behavior changes.